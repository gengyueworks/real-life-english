# 如何贡献

欢迎提 PR。这个仓库的门槛很低：会英语、出过国（或想出国）、愿意把「真的会说的那句话」写下来，就够了。

## 加一个词

1. 打开对应场景的 `scenarios/<场景>.md`
2. 在表格里加一行：`中文 | 英文 | 场景例句`
3. 例句要求：
   - 自然、贴合场景，能直接拿去用（别写教科书式句子）
   - 英文句后加括号写简短中文翻译，方便确认意思
   - 英文词用 **粗体**

## 加一个新场景

1. 复制 `_template.md` 为 `scenarios/<场景名>.md`（文件名用英文小写、连字符，如 `coffee-shop.md`）
2. 在 `README.md` 的场景清单表格里加一行链接
3. 文件开头写一句「这个场景你会在哪用到」

## 格式约定

- 英文单词：**粗体**
- 例句：英文句 + （中文翻译），保持口语、真实
- 不写音标、不写考试语法——除非它对理解场景真的必要
- 一个场景一个文件，别把所有词堆在一个大文件里

## 提交规范

- commit message 写清楚做了啥，例如：`add: restaurant scenario`、`feat: add 12 words to fruit`
- 一次 PR 只做一件事（加一个场景 / 补一批词），方便 review
