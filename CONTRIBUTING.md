# 如何贡献

欢迎提 PR。这个仓库门槛很低：会中英任意一种、出过国（或想出国/来华）、愿意把「真的会说的那句话」写下来，就够了。

仓库是**双向**的：`en/` 给中文使用者出国，`zh/` 给外国人来中国。同一批场景，列顺序相反。

## 加一个词

1. 打开对应场景的文件：
   - 英文版：`en/scenarios/<场景>.md`
   - 中文版：`zh/scenarios/<场景>.md`
2. 在表格里加一行（注意列顺序相反）：
   - `en/`：`中文 | 英文 | 场景例句（英文 + 中文翻译）`
   - `zh/`：`英文 | 中文 | 场景例句（中文 + 英文翻译）`
3. 例句要求：
   - 自然、贴合场景，能直接拿去用（别写教科书式句子）
   - 目标语言句后加括号写另一语言的简短翻译，方便确认意思
   - 目标语言词用 **粗体**

> 如果你只熟悉某一侧语言，只改那一侧也完全 OK；另一侧可以留给懂的人补。

## 加一个新场景

1. 在 `en/scenarios/` 和 `zh/scenarios/` 各建一个同名文件（英文小写、连字符，如 `coffee-shop.md`）
2. 复制 `_template.md` 当起点，按各自列顺序填
3. 在两个 README 的场景清单表格里各加一行链接
4. 每个文件开头写一句「这个场景你会在哪用到」

## 格式约定

- 目标语言单词：**粗体**
- 例句：目标语言句 + （另一种语言翻译），保持口语、真实
- 不写音标、不写考试语法——除非它对理解场景真的必要
- 一个场景一个文件，别把所有词堆在一个大文件里
- 图标列可选：有清晰对应的 emoji 就加在最左，没有就省略整列

## 提交规范

- commit message 写清楚做了啥，例如：`add: restaurant scenario`、`feat: add 12 words to fruit`
- 一次 PR 只做一件事（加一个场景 / 补一批词），方便 review
