# 当前阶段与未来规划（备注）

## 当前：纯文字简易版

本仓库目前是**纯文字、Markdown 简易版**：

- 每个场景 = 一组词表 + 场景例句，配 emoji 图标方便一眼定位；
- 定位是「先有内容、先能用」——实用、好查、可被 PR 扩展；
- **不依赖图片资源**：GitHub 对图片不友好，现阶段刻意不塞图，保持纯文字、轻量、可检索。

这是有意为之的「简版」——先把场景划分、词表、例句这套骨架定扎实，未来视觉化只是「换皮」，底层内容直接复用。

## 未来：视觉化生动版

等条件成熟，会在现有内容骨架上开发更生动的版本：

- **图示化**：每个词配真实场景插画 / 图标 / 照片，告别纯文字；
- **形态可能是**：
  - 网页版（GitHub Pages 或独立站）——卡片式浏览、可检索；
  - 手机里的便捷应用（App / 小程序）——随身查、点开即学；
- **增加视觉化交互**：场景插画、记忆卡片、发音、互动练习等。

> 一句话：现在攒的「场景 + 词 + 例句」是资产，未来不管做成网页还是 App，这套内容都是底座，不用推倒重来。
